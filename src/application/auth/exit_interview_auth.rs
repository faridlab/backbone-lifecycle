//! Auth configuration for ExitInterview handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ExitInterview>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ExitInterviewService;
use crate::domain::entity::ExitInterview;

/// Permission string constants for ExitInterview operations.
pub mod permissions {
    pub const LIST:        &str = "exit_interview:list";
    pub const READ:        &str = "exit_interview:read";
    pub const CREATE:      &str = "exit_interview:create";
    pub const UPDATE:      &str = "exit_interview:update";
    pub const DELETE:      &str = "exit_interview:delete";
    pub const RESTORE:     &str = "exit_interview:restore";
    pub const TRASH:       &str = "exit_interview:trash";
    pub const EMPTY_TRASH: &str = "exit_interview:empty_trash";
    pub const BULK_CREATE: &str = "exit_interview:bulk_create";
    pub const UPSERT:      &str = "exit_interview:upsert";
}

/// Resource policy for ExitInterview — maps CRUD actions to permission strings.
pub struct ExitInterviewPolicy;

#[async_trait]
impl ResourcePolicy<ExitInterview> for ExitInterviewPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ExitInterview,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for ExitInterview handlers.
///
/// Wraps `ExitInterviewService` and checks `ExitInterviewPolicy` before delegating.
pub type ExitInterviewGuard = PermissionGuard<ExitInterview>;

/// Service-integrated permission guard for ExitInterview (for use in generated handlers).
pub type ExitInterviewServiceGuard = ServicePermissionGuard<ExitInterview, ExitInterviewService, ExitInterviewPolicy>;

// <<< CUSTOM
// END CUSTOM
