//! Auth configuration for OnboardingTask handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<OnboardingTask>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::OnboardingTaskService;
use crate::domain::entity::OnboardingTask;

/// Permission string constants for OnboardingTask operations.
pub mod permissions {
    pub const LIST:        &str = "onboarding_task:list";
    pub const READ:        &str = "onboarding_task:read";
    pub const CREATE:      &str = "onboarding_task:create";
    pub const UPDATE:      &str = "onboarding_task:update";
    pub const DELETE:      &str = "onboarding_task:delete";
    pub const RESTORE:     &str = "onboarding_task:restore";
    pub const TRASH:       &str = "onboarding_task:trash";
    pub const EMPTY_TRASH: &str = "onboarding_task:empty_trash";
    pub const BULK_CREATE: &str = "onboarding_task:bulk_create";
    pub const UPSERT:      &str = "onboarding_task:upsert";
}

/// Resource policy for OnboardingTask — maps CRUD actions to permission strings.
pub struct OnboardingTaskPolicy;

#[async_trait]
impl ResourcePolicy<OnboardingTask> for OnboardingTaskPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &OnboardingTask,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for OnboardingTask handlers.
///
/// Wraps `OnboardingTaskService` and checks `OnboardingTaskPolicy` before delegating.
pub type OnboardingTaskGuard = PermissionGuard<OnboardingTask>;

/// Service-integrated permission guard for OnboardingTask (for use in generated handlers).
pub type OnboardingTaskServiceGuard = ServicePermissionGuard<OnboardingTask, OnboardingTaskService, OnboardingTaskPolicy>;

// <<< CUSTOM
// END CUSTOM
