//! Domain events for FinalSettlement entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FinalSettlement;

/// Domain events for FinalSettlement — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FinalSettlement>)` variant.
pub type FinalSettlementEvent = CrudEvent<FinalSettlement>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FinalSettlement events.
pub type FinalSettlementEventPublisher = Arc<dyn CrudEventPublisher<FinalSettlement> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFinalSettlementEventPublisher = NoOpCrudEventPublisher<FinalSettlement>;
