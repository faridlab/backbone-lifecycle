//! Domain events for Promotion entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Promotion;

/// Domain events for Promotion — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Promotion>)` variant.
pub type PromotionEvent = CrudEvent<Promotion>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Promotion events.
pub type PromotionEventPublisher = Arc<dyn CrudEventPublisher<Promotion> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPromotionEventPublisher = NoOpCrudEventPublisher<Promotion>;
