//! Repository trait for ClearanceItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ClearanceItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ClearanceItem, ClearanceStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ClearanceItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ClearanceItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ClearanceItemPaginatedResult {
    pub data: Vec<ClearanceItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ClearanceItemFilter {
    pub company_id: Option<Uuid>,
    pub offboarding_id: Option<Uuid>,
    pub title: Option<String>,
    pub clearer_employee_id: Option<Uuid>,
    pub status: Option<ClearanceStatus>,
}

impl ClearanceItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.offboarding_id.is_some() || self.title.is_some() || self.clearer_employee_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for ClearanceItem aggregate
///
/// This trait defines the persistence contract for ClearanceItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ClearanceItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new clearance_item entity
    async fn save(&self, entity: &ClearanceItem) -> Result<ClearanceItem>;

    /// Find clearance_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ClearanceItem>>;

    /// Find all clearance_item entities
    async fn find_all(&self) -> Result<Vec<ClearanceItem>>;

    /// Update clearance_item by ID
    async fn update(&self, id: &str, entity: &ClearanceItem) -> Result<Option<ClearanceItem>>;

    /// Delete clearance_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List clearance_item with pagination
    async fn list(&self, params: ClearanceItemPaginationParams) -> Result<ClearanceItemPaginatedResult>;

    /// List clearance_item with pagination and filters
    async fn list_with_filters(&self, params: ClearanceItemPaginationParams, filters: ClearanceItemFilter) -> Result<ClearanceItemPaginatedResult>;

    /// Count all clearance_item entities
    async fn count(&self) -> Result<u64>;

    /// Count clearance_item entities matching filters
    async fn count_with_filters(&self, filters: ClearanceItemFilter) -> Result<u64>;

    /// Check if clearance_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete clearance_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted clearance_item by ID
    async fn restore(&self, id: &str) -> Result<Option<ClearanceItem>>;

    /// List soft-deleted clearance_item entities
    async fn list_deleted(&self, params: ClearanceItemPaginationParams) -> Result<ClearanceItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save clearance_item entities
    async fn bulk_save(&self, entities: &[ClearanceItem]) -> Result<Vec<ClearanceItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
