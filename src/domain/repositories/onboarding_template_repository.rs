//! Repository trait for OnboardingTemplate aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OnboardingTemplate aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::OnboardingTemplate;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OnboardingTemplatePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl OnboardingTemplatePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct OnboardingTemplatePaginatedResult {
    pub data: Vec<OnboardingTemplate>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OnboardingTemplateFilter {
    pub name: Option<String>,
    pub description: Option<String>,
    pub is_default: Option<bool>,
    pub is_active: Option<bool>,
}

impl OnboardingTemplateFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.description.is_some() || self.is_default.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for OnboardingTemplate aggregate
///
/// This trait defines the persistence contract for OnboardingTemplate entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OnboardingTemplateRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new onboarding_template entity
    async fn save(&self, entity: &OnboardingTemplate) -> Result<OnboardingTemplate>;

    /// Find onboarding_template by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OnboardingTemplate>>;

    /// Find all onboarding_template entities
    async fn find_all(&self) -> Result<Vec<OnboardingTemplate>>;

    /// Update onboarding_template by ID
    async fn update(&self, id: &str, entity: &OnboardingTemplate) -> Result<Option<OnboardingTemplate>>;

    /// Delete onboarding_template by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List onboarding_template with pagination
    async fn list(&self, params: OnboardingTemplatePaginationParams) -> Result<OnboardingTemplatePaginatedResult>;

    /// List onboarding_template with pagination and filters
    async fn list_with_filters(&self, params: OnboardingTemplatePaginationParams, filters: OnboardingTemplateFilter) -> Result<OnboardingTemplatePaginatedResult>;

    /// Count all onboarding_template entities
    async fn count(&self) -> Result<u64>;

    /// Count onboarding_template entities matching filters
    async fn count_with_filters(&self, filters: OnboardingTemplateFilter) -> Result<u64>;

    /// Check if onboarding_template exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete onboarding_template by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted onboarding_template by ID
    async fn restore(&self, id: &str) -> Result<Option<OnboardingTemplate>>;

    /// List soft-deleted onboarding_template entities
    async fn list_deleted(&self, params: OnboardingTemplatePaginationParams) -> Result<OnboardingTemplatePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save onboarding_template entities
    async fn bulk_save(&self, entities: &[OnboardingTemplate]) -> Result<Vec<OnboardingTemplate>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
