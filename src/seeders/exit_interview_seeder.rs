//! ExitInterview seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ExitInterview data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EXITINTERVIEW SEEDER
// ============================================================================

/// Seeder for exit_interview
pub struct SeedExitInterviewSeeder;

impl SeedExitInterviewSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedExitInterviewSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedExitInterviewSeeder {
    fn name(&self) -> &'static str {
        "SeedExitInterviewSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if lifecycle.exit_interviews table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM lifecycle.exit_interviews")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/exit_interview_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM lifecycle.exit_interviews")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

